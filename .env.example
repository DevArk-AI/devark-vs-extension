# DevArk VS Code Extension Environment Variables
# Copy this file to .env for local development

# Note: Mixpanel tokens are hardcoded in src/config/analytics.config.ts
# Build mode determines which token is used:
#   - npm run watch → staging token
#   - npm run build → production token
